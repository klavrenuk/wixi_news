import type { TableTH } from "@/types/table";
import type { News } from "@/types/news";

export const tableTHNews:TableTH[] = [
    {
        key: 'date',
        name: 'Время'
    },
    {
        key: 'title',
        name: 'Заголовок'
    }
];

export const news:News[] = [

];
